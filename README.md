lienert@climate.unibe.ch
------------------------

My vim configuration, using pathogen... Pretty standard, work in progress
Leader is mapped to space

checkout to folder ~/.vim/ and copy .vimrc to ~/.vimrc
